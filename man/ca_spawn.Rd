% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tasks.R
\name{ca_spawn}
\alias{ca_spawn}
\title{Submit a durable task}
\usage{
ca_spawn(
  db,
  name,
  input = NULL,
  queue = "default",
  id = NULL,
  priority = 0L,
  max_failures = 3L
)
}
\arguments{
\item{db}{A handle returned by \code{\link[=ca_open]{ca_open()}}, \code{\link[=ca_serve]{ca_serve()}}, or \code{\link[=ca_connect]{ca_connect()}}.}

\item{name}{Registered handler name.}

\item{input}{An R value supported by \link{ca_values}. No package-imposed byte
limit applies to input, results, checkpoints, or error text.}

\item{queue}{Queue name.}

\item{id}{Stable task ID (1 to 256 characters), or \code{NULL} to generate a UUID.}

\item{priority}{Integer priority; larger values are claimed first.}

\item{max_failures}{Number of failures, including expired leases, before a
task becomes terminal. Durable sleeps do not consume this budget. Schema
version 1 accepts values from 1 through 1,000,000.}
}
\value{
The task ID, a character scalar.
}
\description{
Reusing an ID with an identical submission is idempotent, even after the task
has completed, so a lost response can be resubmitted safely. A different
submission under that ID raises \code{canard_spawn_conflict}. Equality covers the
native value and its R type descriptor.
}
\details{
A constraint failure is resolved by looking the existing task up rather than
writing again. If no task is found, the original storage error stands; if the
lookup itself fails, its condition carries that error as \code{submission_error}.
}
