% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tasks.R
\name{ca_spawn}
\alias{ca_spawn}
\title{Submit a durable task}
\usage{
ca_spawn(
  db,
  name,
  input = NULL,
  queue = "default",
  id = NULL,
  priority = 0L,
  max_failures = 3L
)
}
\arguments{
\item{db}{A handle returned by \code{\link[=ca_open]{ca_open()}}, \code{\link[=ca_serve]{ca_serve()}}, or \code{\link[=ca_connect]{ca_connect()}}.}

\item{name}{Registered handler name.}

\item{input}{JSON-compatible R value. Values are limited to 1 MiB of JSON.}

\item{queue}{Queue name.}

\item{id}{Stable task ID (1 to 256 characters), or \code{NULL} to generate a UUID.}

\item{priority}{Integer priority; larger values are claimed first.}

\item{max_failures}{Number of failures, including expired leases, before a
task becomes terminal. Durable sleeps do not consume this budget.}
}
\value{
The task ID, a character scalar.
}
\description{
Reusing an ID with the same submission is idempotent, including after
completion. A different submission under that ID raises
\code{canard_spawn_conflict}. Supply a stable ID to recover from a lost submission
response. JSON encoding defines input equality, including object key order.
}
