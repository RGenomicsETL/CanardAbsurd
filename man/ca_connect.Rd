% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/connection.R
\name{ca_connect}
\alias{ca_connect}
\title{Connect to a workflow server}
\usage{
ca_connect(uri = "quack:127.0.0.1:9494", token, extension = NULL)
}
\arguments{
\item{uri}{Quack endpoint, including a port when not using the default.}

\item{token}{Shared authentication token. Quack currently requires at least
four characters. A holder can run any SQL, including file access, with the
server process's privileges; give it only to trusted workers and services.}

\item{extension}{Optional path to a compatible signed Quack extension.
\code{NULL} loads an explicitly preinstalled \code{quack} extension.}
}
\value{
An S7 \code{CanardConnection} handle.
}
\description{
Connects to a \code{\link[=ca_serve]{ca_serve()}} endpoint. The client holds only an in-memory
DuckDB connection, never a second writable handle to the server's file. Quack
clients also load DuckDB's \code{httpfs} extension; install both explicitly,
because connections never download extensions. With \code{extension = NULL} both
come from DuckDB's shared home; a path loads that build plus any
\code{httpfs.duckdb_extension} beside it into session-private storage.
}
