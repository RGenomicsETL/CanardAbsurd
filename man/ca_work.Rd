% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/workflow.R
\name{ca_work}
\alias{ca_work}
\title{Pull and execute R tasks}
\usage{
ca_work(
  db,
  handlers,
  queue = "default",
  max_tasks = Inf,
  poll_seconds = 0.1,
  idle_timeout = Inf,
  lease_seconds = 30,
  worker = paste0("R-", Sys.getpid())
)
}
\arguments{
\item{db}{A handle returned by \code{\link[=ca_open]{ca_open()}}, \code{\link[=ca_serve]{ca_serve()}}, or \code{\link[=ca_connect]{ca_connect()}}.}

\item{handlers}{Named list of functions taking \verb{(input, ctx)}.}

\item{queue}{Queue name.}

\item{max_tasks}{Maximum number of claimed attempts, including failures and
suspensions. \code{Inf} runs without an attempt limit.}

\item{poll_seconds}{Positive delay when no work is found.}

\item{idle_timeout}{Nonnegative seconds without a claim before returning.
\code{Inf} waits indefinitely. Zero drains currently eligible work.}

\item{lease_seconds}{Positive lease duration in seconds. Long-running handlers
must explicitly heartbeat before this interval expires.}

\item{worker}{Worker label used for inspection; lease tokens establish ownership.}
}
\value{
The number of executed attempts, invisibly.
}
\description{
Each worker executes one attempt at a time. Scale with independent R processes
connected through Quack. Only registered handler names are claimed. Heartbeats
occur at step boundaries and through \code{\link[=ca_heartbeat]{ca_heartbeat()}}, not on a background R
thread. The worker never terminates its host process on lease loss.
}
