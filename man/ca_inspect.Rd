% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tasks.R
\name{ca_inspect}
\alias{ca_inspect}
\title{Inspect a task}
\usage{
ca_inspect(db, id)
}
\arguments{
\item{db}{A handle returned by \code{\link[=ca_open]{ca_open()}}, \code{\link[=ca_serve]{ca_serve()}}, or \code{\link[=ca_connect]{ca_connect()}}.}

\item{id}{Stable task ID (1 to 256 characters), or \code{NULL} to generate a UUID.}
}
\value{
A named list containing task state, counters, timestamps, R input
and result, and checkpoint records; \code{NULL} for an unknown ID. Step records
contain \code{kind} and \code{value}; sleep records contain \code{kind}.
}
\description{
Reads one task's metadata, then its stored values in a second query, so work
saved in between appears on the next call. Stored values are immutable under
the package protocol; editing payloads directly breaks that. Use \code{\link[=ca_tasks]{ca_tasks()}}
to poll many tasks and \code{\link[=ca_result]{ca_result()}} for a result on its own.
}
