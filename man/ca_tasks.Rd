% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tasks.R
\name{ca_tasks}
\alias{ca_tasks}
\title{List task metadata}
\usage{
ca_tasks(db, queue = NULL, state = NULL, id = NULL, limit = 100L)
}
\arguments{
\item{db}{A handle returned by \code{\link[=ca_open]{ca_open()}}, \code{\link[=ca_serve]{ca_serve()}}, or \code{\link[=ca_connect]{ca_connect()}}.}

\item{queue}{Optional queue name.}

\item{state}{Optional character vector of task states: \code{"ready"},
\code{"running"}, \code{"completed"}, \code{"failed"}, or \code{"cancelled"}.}

\item{id}{Optional character vector of task IDs.}

\item{limit}{Maximum number of rows, most recently updated first.}
}
\value{
A data frame with one row per task: \code{id}, \code{queue}, \code{name}, \code{state},
\code{priority}, \code{attempt}, \code{failures}, \code{max_failures}, \code{available_at}, \code{worker},
\code{lease_until}, \code{error}, \code{created_at}, and \code{updated_at}.
}
\description{
Reads state, counters, ownership, and timestamps for many tasks in one
query, without inputs, results, or checkpoint values. Use it to poll a jobs
view; use \code{\link[=ca_result]{ca_result()}} or \code{\link[=ca_inspect]{ca_inspect()}} for one task's values.
}
